import { defineSection } from './define';

export const maintenance = defineSection(
  { videoLabel: 'Optional short video (MP4, MOV or WebM, up to 50 MB)' },
  { videoLabel: 'Valinnainen lyhyt video (MP4, MOV tai WebM, enintään 50 Mt)' },
);
